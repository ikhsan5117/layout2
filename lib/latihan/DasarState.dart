import 'package:flutter/material.dart';

class Dasarstate extends StatelessWidget {
  const Dasarstate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}